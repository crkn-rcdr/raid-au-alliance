# All message keys are inherited from raid-custom's messages_en.properties
# (message bundles merge per-key across the parent chain, unlike CSS/FTL
# which resolve as whole files). Add a key here only when ardc-branding's
# value genuinely needs to differ from raid-custom's default.
