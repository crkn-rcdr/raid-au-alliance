parent=raid-custom
import=common/keycloak

# Styles
# login.css is inherited from raid-custom (no local copy in this theme);
# login-ardc.css layers on top with only genuine ARDC-specific overrides.
styles=css/login.css css/login-ardc.css